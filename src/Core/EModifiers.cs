namespace DataPuller.Core
{
    public enum EModifiers
    {
        //noFail = -50,
        noBombs = -10,
        noWalls = -5,
        noArrows = -30,
        ghostNotes = 11,
        zenMode = -100,
        slowerSong = -30,
        superFastSong = 10
    }
}
